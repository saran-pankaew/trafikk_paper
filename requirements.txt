# TRAFIKK — Python modules

celios==0.1.0

git+https://github.com/druglogics/drexpa.git@main#egg=drexpa
git+https://github.com/druglogics/oris.git@main#egg=oris
git+https://github.com/druglogics/siflex.git@main#egg=siflex
git+https://github.com/ViviamSB/SYNCO.git@main#egg=synco

# NOTE:
# Gitsbe is Java-based and is not installed with pip.
# Use the prebuilt JAR provided with the reproducibility files.
# - Or download Gitsbe from https://github.com/druglogics/gitsbe (Java 11+ required)

# After a successful install, pin exact versions:
#   pip freeze > requirements-locked.txt
